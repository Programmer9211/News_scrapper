class NewsModel {
  late String title, subtitle, imageUrl, articleLink;

  NewsModel(
      {required this.title,
      required this.subtitle,
      required this.articleLink,
      required this.imageUrl});
}
